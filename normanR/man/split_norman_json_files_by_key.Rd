% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_norman_json_files_by_key.R
\name{split_norman_json_files_by_key}
\alias{split_norman_json_files_by_key}
\title{Split JSON Files Based on Unique Key Values}
\usage{
split_norman_json_files_by_key(
  input_dir,
  output_dir,
  split_key,
  data_path = NULL
)
}
\arguments{
\item{input_dir}{Character. Path to the directory containing source JSON files.}

\item{output_dir}{Character. Path to the directory where split files will be saved.}

\item{split_key}{Character. The name of the key/field used to isolate unique values
(e.g., "Name of country" or "Sample matrix").}

\item{data_path}{Character. If the JSON has a nested structure (like Norman API),
provide the name of the element containing the list of records (e.g., "Data").
Defaults to NULL (assumes the JSON is a top-level list).}
}
\value{
Logical. Returns TRUE if the operation completes successfully.
}
\description{
This function scans a source directory for JSON files, loads them, and splits
each file into multiple smaller JSON files based on the unique values of a
specified key. Each unique value results in a separate file.
}
\details{
The function creates the output directory if it does not exist. Filenames are
generated using the pattern: original_name.json.
}
