% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_norman_fields.R
\name{extract_norman_fields}
\alias{extract_norman_fields}
\title{Extract Specific Fields from Nested Norman List}
\usage{
extract_norman_fields(data_list, field_names)
}
\arguments{
\item{data_list}{A list of lists (the standard output from JSON parsing).}

\item{field_names}{A character vector of field names to extract (e.g., c("E-mail", "City")).}
}
\value{
A data.frame where columns are the requested fields and rows correspond
to the items in the input list. Missing values are filled with NA.
}
\description{
This function iterates through a list of nested data (e.g., from the Norman API)
and extracts values for specific field names provided by the user.
It uses a recursive search to find the fields, meaning the user does not need
to know the full path (e.g., just "City" instead of "Data source$Organisation$City").
}
\examples{
\dontrun{
  fields <- c("id", "Name", "City", "Value", "Unit")
  df <- extract_norman_fields(data_cas_1$Data, fields)
  print(df)
}

}
